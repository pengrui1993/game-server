package media.ffmpeg.refactor;

import media.ffmpeg.net.MP2SoundServerRefactor;

public class Mp2SoundServer extends MP2SoundServerRefactor {
    public Mp2SoundServer(int port) {
        super(port);
    }
}
